#include<stdio.h>
int  main()
{
    float a,b,c;
    printf("Enter number of a:");
    scanf("%f",&a);
    printf("Enter number of b:");
    scanf("%f",&b);
    c=a/b;
    printf("%f / %f = %f",a,b,c);
    return 0;
}