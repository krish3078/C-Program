#include<stdio.h>
int main()
{
     float  celsius,fahrenheit;
    printf("Enter temperature in celsius:");
    scanf("%f",&celsius);
    fahrenheit = (9.0/5.0*celsius) + 32;
    printf("Temperature in farenheit:  9 / 5 * %f    + 32 = %f \n",celsius,fahrenheit);
    return 0;
}