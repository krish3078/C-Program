#include<stdio.h>
int main()
{
    float r,a;
    printf("Enter the radius of the circle:");
    scanf("%f",&r);
    a = (22.0/7.0) * r * r ;
    printf("Area of the circle is : (22.0/7.0) * %f * %f = %f\n",r,r,a);
    return 0;

}